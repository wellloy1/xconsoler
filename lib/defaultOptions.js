import { LOG_LEVELS } from './logLevels.js';

export const DEFAULT_OPTIONS = {
  prefix: null,
  active: true,
  time: 'ls',
  timeColor: true,
  levels: LOG_LEVELS,
  colors: true,
  async: false,
};
